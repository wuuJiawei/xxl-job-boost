<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.job.admin.mapper.XxlJobAlarmRuleMapper">

    <resultMap id="XxlJobAlarmRule" type="com.xxl.job.admin.model.XxlJobAlarmRule">
        <result column="id" property="id" />
        <result column="name" property="name" />
        <result column="job_group" property="jobGroup" />
        <result column="job_id" property="jobId" />
        <result column="alarm_event" property="alarmEvent" />
        <result column="channel_ids" property="channelIds" />
        <result column="enabled" property="enabled" />
        <result column="remark" property="remark" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        t.id,
        t.name,
        t.job_group,
        t.job_id,
        t.alarm_event,
        t.channel_ids,
        t.enabled,
        t.remark,
        t.update_time
    </sql>

    <select id="pageList" resultMap="XxlJobAlarmRule">
        SELECT <include refid="Base_Column_List" />
        FROM xxl_job_alarm_rule t
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            AND (t.job_id IS NULL OR t.job_id = 0)
            <if test="jobGroupIds != null and jobGroupIds.size() > 0">
                AND t.job_group IN
                <foreach collection="jobGroupIds" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="jobGroup gt 0">
                AND t.job_group = #{jobGroup}
            </if>
            <if test="alarmEvent != null and alarmEvent != ''">
                AND t.alarm_event = #{alarmEvent}
            </if>
            <if test="enabled gte 0">
                AND t.enabled = #{enabled}
            </if>
        </trim>
        ORDER BY t.id DESC
        LIMIT #{offset}, #{pagesize}
    </select>

    <select id="pageListCount" resultType="int">
        SELECT count(1)
        FROM xxl_job_alarm_rule t
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            AND (t.job_id IS NULL OR t.job_id = 0)
            <if test="jobGroupIds != null and jobGroupIds.size() > 0">
                AND t.job_group IN
                <foreach collection="jobGroupIds" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="jobGroup gt 0">
                AND t.job_group = #{jobGroup}
            </if>
            <if test="alarmEvent != null and alarmEvent != ''">
                AND t.alarm_event = #{alarmEvent}
            </if>
            <if test="enabled gte 0">
                AND t.enabled = #{enabled}
            </if>
        </trim>
    </select>

    <insert id="save" parameterType="com.xxl.job.admin.model.XxlJobAlarmRule" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO xxl_job_alarm_rule (
            name,
            job_group,
            job_id,
            alarm_event,
            channel_ids,
            enabled,
            remark,
            update_time
        ) VALUES (
            #{name},
            #{jobGroup},
            #{jobId},
            #{alarmEvent},
            #{channelIds},
            #{enabled},
            #{remark},
            #{updateTime}
        )
    </insert>

    <update id="update" parameterType="com.xxl.job.admin.model.XxlJobAlarmRule">
        UPDATE xxl_job_alarm_rule
        SET
            name = #{name},
            job_group = #{jobGroup},
            job_id = #{jobId},
            alarm_event = #{alarmEvent},
            channel_ids = #{channelIds},
            enabled = #{enabled},
            remark = #{remark},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="remove">
        DELETE FROM xxl_job_alarm_rule
        WHERE id = #{id}
    </delete>

    <select id="load" resultMap="XxlJobAlarmRule">
        SELECT <include refid="Base_Column_List" />
        FROM xxl_job_alarm_rule t
        WHERE t.id = #{id}
    </select>

    <select id="findExecutorDefaults" resultMap="XxlJobAlarmRule">
        SELECT <include refid="Base_Column_List" />
        FROM xxl_job_alarm_rule t
        WHERE t.job_group = #{jobGroup}
          AND (t.job_id IS NULL OR t.job_id = 0)
        ORDER BY t.id ASC
    </select>

    <select id="findEnabledExecutorDefaults" resultMap="XxlJobAlarmRule">
        SELECT <include refid="Base_Column_List" />
        FROM xxl_job_alarm_rule t
        WHERE t.enabled = 1
          AND t.job_group = #{jobGroup}
          AND t.alarm_event = #{alarmEvent}
          AND (t.job_id IS NULL OR t.job_id = 0)
        ORDER BY t.id DESC
    </select>

    <delete id="removeExecutorDefaults">
        DELETE FROM xxl_job_alarm_rule
        WHERE job_group = #{jobGroup}
          AND (job_id IS NULL OR job_id = 0)
    </delete>

    <delete id="removeByJobGroup">
        DELETE FROM xxl_job_alarm_rule
        WHERE job_group = #{jobGroup}
    </delete>

</mapper>
